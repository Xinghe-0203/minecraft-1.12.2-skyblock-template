# 项目进度追踪

## 目标
维护和优化 Paper 1.12.2 星空岛 Skyblock 服务器模板，包括中文汉化、配置加固、Bug 修复、文档更新和 Git 仓库清理。
服务器通过 FRP 对外发布，所有玩家以 127.0.0.1 连接，离线模式 + AuthMe 认证。

## 已完成

### P0 - 严重 Bug 修复
- [x] WorldGuard `config.yml` 损坏的 key `fake-player-build-over这个ide` → `fake-player-build-override`
- [x] Essentials `config.yml` 孤立的 `kits:` block（YAML 语法残留）
- [x] Spigot `replace-commands` 移除 `setblock`（与 WorldEdit 冲突）
- [x] ASkyBlock `checkupdates: false`（关闭更新检查，避免 Watchdog 线程挂死）
- [x] FastLogin `messages.yml` 多行单引号字符串修复（`\n` 换行）
- [x] BossShop `messages.yml` 重复根 key `PlayerNotFound`（缩进覆盖中文消息）
- [x] `bukkit.yml` `aliases: now-in-commands.yml`（Paper 实际行为，已标记）
- [x] `start.sh` / `start.bat` Java 版本检查更新（Java 21 警告而非阻止）
- [x] QuickShop `messages.yml` `bypassing-lock` / `that-is-locked` 缩进修复
- [x] `help.yml` YAML 锚点语法修复（`&e` → `'&e'`）
- [x] FastLogin `messages.yml` `invalid-requst` → `invalid-request`

### P1 - 配置修复与汉化
- [x] 全部 Spigot 玩家消息汉化（6 条），Essentials motd/book/kits/help 汉化
- [x] QuickShop/BossShop/SkinsRestorer/AuthMe/FastLogin/ASkyBlock 消息汉化
- [x] Essentials 优化（auto-item-clean, teleport-invulnerability, TPR 参数, sethome-multiple）
- [x] AuthMe 加固（enableTempban, emailVerification, enablePermissionCheck）
- [x] WorldEdit 普通玩家上限（50000 blocks, radius 100）
- [x] server.properties 基础配置（online-mode, difficulty, snooper-enabled）
- [x] 恢复 `online-mode=false` + AuthMe（FRP 场景）
- [x] Essentials `sethome-multiple` 组名 `staff` → `admin`
- [x] Essentials kits `digspeed` → `efficiency`，daxe:780 → daxe
- [x] Essentials worth `carrotitem`/`potatoitem` → `carrot`/`potato`
- [x] QuickShop `display-items: false`（1.12.2 复制漏洞）
- [x] wepif.yml 清理：移除多余 resolver，`admins` → `admin`
- [x] Spigot `tab-complete: -1`（非 OP 无法 Tab 补全）
- [x] Essentials tpr.yml 排除 1.13+ 海洋生物群系
- [x] AuthMe hooks 修复（multiverse/placeholderapi false）
- [x] WorldEdit `max-polygonal-points` 参数交换，`disallowed-blocks` 去重
- [x] ASkyBlock `blockvalues.yml` 重复 key `ACACIA_STAIRS`

### P2 - 仓库清理与文档
- [x] 清理 wepif.yml、空 permissions.yml、重写 README.md 和 命令大全.md
- [x] 扩展 .gitignore（世界目录、运行时数据、敏感文件）
- [x] 从 Git 追踪中移除 47 个运行时/自动生成文件
- [x] WorldGuard wand ID 334 → 288（羽毛）
- [x] server.properties `spawn-protection: 16` → `0`
- [x] paper.yml `per-player-mob-spawns: true` → `false`
- [x] LuckPerms `messaging-service: auto` → `notsql`
- [x] AuthMe `MysqlWindowsPath` 清空
- [x] README Java 21 描述更新
- [x] ProtocolLib 5.3.0 → 4.8.0
- [x] start.sh 添加 `--add-opens` JVM 参数
- [x] WorldGuard wand ID 334 → 288（羽毛）
- [x] 完全移除 FastLogin（AuthMe 接管全部认证）

## 进行中
（无）

## 已阻塞
（无）

## 关键决策
- FRP 转发服务器 → offline-mode + AuthMe（非 Mojang 会话服务器）
- ProtocolLib 4.8.0（兼容 FastLogin 1.10，需 --add-opens JVM 参数）
- AuthMe enablePermissionCheck = false（LuckPerms default 组可能不存在）
- ASkyBlock `createnether: false`（共享下界）
- Paper 将 `bukkit.yml aliases: {}` 重写为 `now-in-commands.yml`（不崩溃）
- FastLogin 被完全移除：其 ProtocolLib 监听器在 autoRegister/autoLogin 关闭后仍会拦截所有连接并调用 Mojang API，导致离线 FRP 玩家被断开连接。AuthMe 已处理全部认证需求。

## 下一步
- 重启服务器应用所有 YAML 配置更改
- 测试 `/spawn`、`/tpr`、QuickShop、AuthMe 登录流程
- 检查 LuckPerms H2 数据库是否已有权限组

## 关键文件
（略，参见 GH commit history）
